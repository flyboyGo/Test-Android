<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chapter09_broadcastreceiver_max">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BaseDemo">
        <activity
            android:name=".DynamicActivity"
            android:exported="true" />
        <activity
            android:name=".SecondActivity"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 静态注册广播接收器 -->
        <receiver android:name=".receiver.OrderABroadCastReceiver"
            android:exported="true">
            <intent-filter android:priority="100">
                <action android:name="order_broadcast_receiver"/>
            </intent-filter>
        </receiver>

        <receiver android:name=".receiver.OrderBBroadCastReceiver"
            android:exported="true">
            <intent-filter android:priority="50">
                <action android:name="order_broadcast_receiver"/>
            </intent-filter>
        </receiver>

        <receiver
            android:name=".receiver.StaticBroadcastReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="static_broadcast_receiver" />
                <!-- 开机完成广播 -->
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <!-- 设置时间完成广播 -->
                <action android:name="android.intent.action.TIME_SET" />
            </intent-filter>
        </receiver>

    </application>

</manifest>